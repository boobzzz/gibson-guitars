import classes from './Main.module.css';

export const Main = () => {
    return (
        <main>
            <section>
                <div className={classes.List}>
                    List
                </div>
            </section>
        </main>
    )
}